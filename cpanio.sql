CREATE TABLE once_a_day   ( contest text, rank int, author text, count int, active boolean, safe boolean );
CREATE TABLE once_a_month ( contest text, rank int, author text, count int, active boolean, safe boolean );
CREATE TABLE once_a_week  ( contest text, rank int, author text, count int, active boolean, safe boolean );
